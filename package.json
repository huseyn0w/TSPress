{
  "name": "cmstack-ts",
  "version": "0.0.0",
  "private": true,
  "description": "Cmstack-TS — an open-source,  CMS built entirely in TypeScript.",
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "e2e": "playwright test",
    "typecheck": "pnpm -r --filter \"./packages/*\" build && pnpm -r typecheck",
    "db:generate": "pnpm --filter @cmstack-ts/db generate",
    "db:migrate": "pnpm --filter @cmstack-ts/db migrate",
    "db:seed": "pnpm --filter @cmstack-ts/db seed"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@playwright/test": "1.49.1",
    "@vitest/coverage-v8": "2.1.8",
    "typescript": "5.7.3",
    "vitest": "2.1.8"
  }
}
